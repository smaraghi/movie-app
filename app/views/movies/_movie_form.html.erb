<%= form_for @movie do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  Cinema: <%= f.collection_select :cinema_name, Cinema.all, :name, :name %>
  Viewer: <%= f.collection_select :viewer_name, Viewer.all, :first_name, :first_name %>
  <%= f.submit %>
<% end %> 